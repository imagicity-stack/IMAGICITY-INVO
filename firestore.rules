rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Restrict all reads and writes to authenticated users.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
